apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sts-task.fullname" . }}-test-connection"
  labels:
    {{- include "sts-task.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sts-task.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
